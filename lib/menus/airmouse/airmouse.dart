import 'package:flutter/material.dart';

class AirMousePage extends StatelessWidget {
  const AirMousePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
